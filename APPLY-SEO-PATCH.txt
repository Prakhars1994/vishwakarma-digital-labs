Vishwakarma Digital Labs — Production SEO Patch V10

Updated files:
- app/layout.tsx
- app/page.tsx
- app/robots.ts
- app/sitemap.ts
- app/manifest.ts
- app/opengraph-image.tsx
- app/demos/layout.tsx
- app/services/[slug]/page.tsx
- lib/site.ts

What this patch does:
- fixes page-specific canonical URLs
- strengthens homepage and service metadata
- adds Open Graph / social sharing image
- adds Organization, WebSite, FAQ, ItemList and Service structured data
- keeps fictional portfolio demos out of the search index
- removes demo routes from sitemap
- keeps /api/ out of crawling
- adds web app manifest
- preserves Vercel Analytics in the root layout

Apply this ZIP into the existing project root. It is a patch, not a full replacement project.
